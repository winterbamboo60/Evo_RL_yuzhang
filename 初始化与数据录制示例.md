```bash
Step0. 主从臂都设置成从动模式

Step1. 切换环境
conda activate evorl

Step2. 查看所有CAN口号
cd ~/VLA/lerobot/src && bash piper_sdk/find_all_can_port.sh

Step3. 初始化2个CAN口
lerobot-setup-can --mode=setup --interfaces=can0,can1

Step4. 模式测试，需要能看到持续输出的数据
lerobot-setup-can --mode=test --interfaces=can0
lerobot-setup-can --mode=test --interfaces=can1

Step5. 遥控操作，测试机械臂硬件是否正常，注意修改CAN口
lerobot-teleoperate \
  --robot.type=piper_follower \
  --robot.port=can1 \
  --robot.id=my_piper_follower \
  --robot.require_calibration=false \
  --teleop.type=piper_leader \
  --teleop.port=can0 \
  --teleop.id=my_piper_leader \
  --teleop.require_calibration=false

Step6. 录制数据，注意修改CAN口，每次注意修改task、camera id、dataset.root，policy.path无所谓，当前录制数据不会加载模型
lerobot-human-inloop-record \
  --robot.type=piper_follower \
  --robot.port=can1 \
  --robot.id=my_piper_follower \
  --robot.speed_ratio=50 \
  --robot.cameras="{wrist: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30}}" \
  --teleop.type=piper_leader \
  --teleop.port=can0 \
  --teleop.id=my_piper_leader \
  --teleop.command_speed_ratio=50 \
  --dataset.root=/home/ghr/VLA/package_data/single/evo-rl/mutipackage_subtask1_5 \
  --dataset.repo_id=local_data \
  --dataset.single_task="Grab the package and place it on the pallet." \
  --dataset.num_episodes=50 \
  --dataset.episode_time_s=120 \
  --dataset.reset_time_s=1 \
  --dataset.push_to_hub=false \
  --resume=false \
  --display_data=true

# 在线采样
conda activate evo-rl-yz-loop
cd /home/ghr/yuzhang/Evo-RL-data/src/lerobot/scripts
lerobot-human-inloop-record \
  --robot.type=piper_follower \
  --robot.port=can1 \
  --robot.id=my_piper_follower \
  --robot.speed_ratio=50 \
  --robot.cameras="{wrist: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30}}" \
  --teleop.type=piper_leader \
  --teleop.port=can0 \
  --teleop.id=my_piper_leader \
  --teleop.command_speed_ratio=50 \
  --dataset.root=/home/ghr/yuzhang/datasets/loop_output/package_scan_model_v6_260604_use_3 \
  --dataset.repo_id=local_data \
  --dataset.single_task="Flip the package if the barcode is not facing up." \
  --dataset.num_episodes=50 \
  --dataset.episode_time_s=120 \
  --dataset.reset_time_s=1 \
  --dataset.push_to_hub=False \
  --policy.path=/home/ghr/mm/package_scan_model_v6 \
  --resume=false \f
  --display_data=true


Grab the package and place it on the pallet.
Flip the package if the barcode is not facing up.
Grab the package and place it into the box.


Step7. 回到初始位置，速度较快
mmmitre

录制指令：
esc :停止录制, -> :保存当前 episode,  <- :重录

```